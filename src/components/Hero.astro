<section
  id="hero"
  class="relative min-h-screen bg-charcoal flex flex-col items-center justify-center px-6 pt-16 overflow-hidden"
>
  <!-- Background gradient -->
  <div class="absolute inset-0 bg-gradient-to-br from-charcoal via-charcoal-90 to-black opacity-80 pointer-events-none"></div>

  <!-- Gold accent bar -->
  <div class="absolute top-0 left-0 right-0 h-1 bg-gold"></div>

  <div class="relative z-10 max-w-5xl mx-auto text-center">

    <p class="text-gold text-sm font-semibold uppercase tracking-[0.25em] mb-6 reveal">
      Licensed Real Estate Agent
    </p>

    <h1 class="font-display text-[clamp(4rem,12vw,9rem)] leading-none text-white uppercase mb-6 reveal">
      Your Dream Home<br/>
      <span class="text-gold">Starts Here</span>
    </h1>

    <p class="text-white/60 text-lg md:text-xl max-w-2xl mx-auto mb-10 leading-relaxed reveal">
      Expert guidance, unmatched market knowledge, and a relentless
      commitment to getting you the best deal — every time.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center reveal">
      <a
        href="#contact"
        class="bg-gold hover:bg-gold-hover text-charcoal font-bold uppercase tracking-widest px-8 py-4 transition-colors text-sm"
      >
        Get In Touch
      </a>
      <a
        href="#about"
        class="border border-white/30 hover:border-gold text-white hover:text-gold font-bold uppercase tracking-widest px-8 py-4 transition-colors text-sm"
      >
        Learn More
      </a>
    </div>

  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 text-white/30 animate-bounce">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 5v14M5 12l7 7 7-7"/>
    </svg>
  </div>

</section>
