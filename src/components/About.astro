---
import { getEntry } from 'astro:content';

const entry = await getEntry('about', 'bio');
const { heading, stats } = entry.data;
const { Content } = await entry.render();
---

<section id="about" class="bg-offwhite py-24 px-6">
  <div class="max-w-6xl mx-auto grid md:grid-cols-2 gap-16 items-center">

    <!-- Photo column -->
    <div class="reveal">
      <div class="aspect-[3/4] bg-lightgray relative overflow-hidden">
        <!-- Replace with actual headshot -->
        <div class="w-full h-full bg-gradient-to-br from-charcoal-90 to-charcoal flex items-center justify-center">
          <span class="font-display text-6xl text-gold/30">CK</span>
        </div>
        <!-- Gold accent border -->
        <div class="absolute inset-0 border-4 border-gold translate-x-4 translate-y-4 -z-10"></div>
      </div>
    </div>

    <!-- Content column -->
    <div>
      <p class="text-gold text-sm font-semibold uppercase tracking-[0.2em] mb-4 reveal">
        About Me
      </p>
      <h2 class="font-display text-[clamp(3rem,6vw,5rem)] leading-none text-charcoal uppercase mb-6 reveal">
        {heading.split('\n').map((line, i, arr) => (
          <>{line}{i < arr.length - 1 && <br/>}</>
        ))}
      </h2>
      <div class="space-y-4 text-charcoal/70 leading-relaxed reveal [&>p]:mb-0">
        <Content />
      </div>

      <!-- Stats row -->
      <div class="grid grid-cols-3 gap-6 mt-10 reveal">
        {stats.map(stat => (
          <div>
            <div class="font-display text-4xl text-gold">{stat.number}</div>
            <div class="text-xs text-charcoal/50 uppercase tracking-widest mt-1">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>

  </div>
</section>
